<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
	<% @users.each do |user| %>
		<li>
			<!--looks for _user.html.erb partial -->
			<%= render user %>
		</li>
	<% end %>
</ul>

<%= will_paginate %>