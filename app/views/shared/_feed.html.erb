<% if @feed_items.any? %>
	<ol class="microposts">
		<!--As stated in controller, this automatically renders _micropost.html.erb b/c of Micropost class in feed method from users_controller -->
		<%= render @feed_items %>
	</ol>
	<%= will_paginate @feed_items %>
<% end %>